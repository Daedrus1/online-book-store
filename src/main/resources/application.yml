spring:
  datasource:
    url: jdbc:mysql://localhost:3306/your_schema?useSSL=false&serverTimezone=UTC
    username: root
    password: Pablo1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml
